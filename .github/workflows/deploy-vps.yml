name: Deploy Frontend to VPS

on:
  push:
    branches: [ "master" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy frontend on VPS
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            cd /opt/hockeyplanner/frontend-src
            git pull origin master
            npm ci
            npm run version:sync
            APP_VERSION="$(cat VERSION | tr -d '[:space:]')"
            APP_BUILD_TIME="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"
            REACT_APP_API_BASE=/api REACT_APP_VERSION="$APP_VERSION" REACT_APP_BUILD_TIME="$APP_BUILD_TIME" npm run build
            rm -rf /opt/hockeyplanner/frontend/build
            cp -r /opt/hockeyplanner/frontend-src/build /opt/hockeyplanner/frontend/build
            cd /opt/hockeyplanner
            docker compose restart nginx
            docker compose ps
